# Volumes to include in the module backup:
#   odoo-pgdata  PostgreSQL data directory (the databases)
#   odoo-data    Odoo filestore (attachments, assets)
#   odoo-addons  extra / OCA add-ons dropped in by the admin
volumes/odoo-pgdata
volumes/odoo-data
volumes/odoo-addons
